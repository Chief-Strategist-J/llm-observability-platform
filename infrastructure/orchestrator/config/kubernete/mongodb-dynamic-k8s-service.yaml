apiVersion: v1
kind: Service
metadata:
  name: mongodb-${INSTANCE_ID}
  labels:
    app: mongodb
    instance: "${INSTANCE_ID}"
spec:
  type: ClusterIP
  ports:
  - port: 27017
    targetPort: 27017
    protocol: TCP
    name: mongodb
  selector:
    app: mongodb
    instance: "${INSTANCE_ID}"
