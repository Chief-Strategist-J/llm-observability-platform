apiVersion: apps/v1
kind: Deployment
metadata:
  name: argocd-repo-${INSTANCE_ID}
  namespace: ${NAMESPACE}
  labels:
    app: argocd-repo
    instance: "${INSTANCE_ID}"
spec:
  replicas: 1
  selector:
    matchLabels:
      app: argocd-repo
      instance: "${INSTANCE_ID}"
  template:
    metadata:
      labels:
        app: argocd-repo
        instance: "${INSTANCE_ID}"
    spec:
      containers:
      - name: argocd-repo-server
        image: quay.io/argoproj/argocd:latest
        command:
        - argocd-repo-server
        ports:
        - containerPort: 8081
          name: server
        - containerPort: 8084
          name: metrics
        env:
        - name: INSTANCE_ID
          value: "${INSTANCE_ID}"
        resources:
          limits:
            memory: 512Mi
            cpu: 500m
          requests:
            memory: 256Mi
            cpu: 250m
        livenessProbe:
          httpGet:
            path: /healthz
            port: 8084
          initialDelaySeconds: 30
          periodSeconds: 30
        readinessProbe:
          httpGet:
            path: /healthz
            port: 8084
          initialDelaySeconds: 10
          periodSeconds: 10
