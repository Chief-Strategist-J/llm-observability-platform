name: Generate Protobuf SDKs (Trigger)

on:
  push:
    branches: [ main, master ]
    paths:
      - 'packages/python/instrumentation-sdk/contracts/proto/v1/**'
      - 'buf.gen.yaml'
  workflow_dispatch:

jobs:
  call-generate:
    uses: ./.github/workflows/reusable-generate-proto-sdks.yml
    secrets: inherit
