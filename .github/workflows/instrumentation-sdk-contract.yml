name: Contract Enforcement (Trigger)

on:
  push:
    paths:
      - 'packages/python/instrumentation-sdk/**'
  pull_request:
    paths:
      - 'packages/python/instrumentation-sdk/**'

jobs:
  call-contract-check:
    uses: ./.github/workflows/reusable-contract-enforcement.yml
    secrets: inherit
