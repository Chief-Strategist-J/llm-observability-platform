# Instructions

- Following Playwright test failed.
- Explain why, be concise, respect Playwright best practices.
- Provide a snippet of code with the fix, if possible.

# Test info

- Name: e2e/sequential-user-flow.journey.spec.ts >> Sequential User Journey: Registration -> Login -> Dashboard >> Step 3: Dashboard Phase — Workspace Filters & Telemetry Visualizations
- Location: tests/automation/e2e/sequential-user-flow.journey.spec.ts:69:7

# Error details

```
Error: locator.fill: Target page, context or browser has been closed
Call log:
  - waiting for locator('input[placeholder*="Search"]')

```

# Test source

```ts
  1  | import { Page, expect } from '@playwright/test';
  2  | import { BasePage } from '../base.page';
  3  | 
  4  | export class DashboardPage extends BasePage {
  5  |   readonly searchInput = this.page.locator('input[placeholder*="Search"]');
  6  |   readonly emptyStateContainer = this.page.locator('[data-testid="empty-telemetry-state"], .empty-state');
  7  |   readonly filterBadge = this.page.locator('[data-testid="active-filter-badge"]');
  8  | 
  9  |   constructor(page: Page) {
  10 |     super(page);
  11 |   }
  12 | 
  13 |   async goto(): Promise<void> {
  14 |     await this.navigateTo('/');
  15 |   }
  16 | 
  17 |   async applySearchFilter(query: string): Promise<void> {
> 18 |     await this.searchInput.fill(query);
     |                            ^ Error: locator.fill: Target page, context or browser has been closed
  19 |     await this.page.waitForLoadState('networkidle');
  20 |   }
  21 | 
  22 |   async assertEmptyStateVisible(): Promise<void> {
  23 |     await expect(this.emptyStateContainer.first()).toBeVisible();
  24 |   }
  25 | }
  26 | 
```