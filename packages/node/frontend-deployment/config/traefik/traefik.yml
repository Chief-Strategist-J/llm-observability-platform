entryPoints:
  web:
    address: ":80"
  dashboard:
    address: ":8080"

api:
  insecure: true
  dashboard: true

providers:
  docker:
    exposedByDefault: false
    endpoint: "unix:///var/run/docker.sock"
  file:
    directory: "/etc/traefik/dynamic"
    watch: true
