http:
  routers:
    auth-router:
      rule: "PathPrefix(`/api/v1/auth`)"
      service: "auth-service"
      entryPoints:
        - "web"

  services:
    auth-service:
      loadBalancer:
        servers:
          - url: "http://host.docker.internal:3001"
