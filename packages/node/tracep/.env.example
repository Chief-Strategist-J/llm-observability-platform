# tracep SDK environment variables
# Copy to .env and fill in values

# Base URL of the tracep ingest server (OTLP/HTTP endpoint)
TRACEP_ENDPOINT=http://localhost:4318

# Bearer token to authenticate with the tracep server
TRACEP_API_KEY=

# Service name reported in all traces
TRACEP_SERVICE=my-service
