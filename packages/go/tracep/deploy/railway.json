{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "build/Dockerfile",
    "context": "."
  },
  "deploy": {
    "startCommand": "/app/traceserver",
    "restartPolicyType": "ON_FAILURE"
  }
}
