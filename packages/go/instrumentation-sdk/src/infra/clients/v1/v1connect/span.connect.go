// Code generated by protoc-gen-connect-go. DO NOT EDIT.
//
// Source: span.proto

package v1connect

import (
	connect "connectrpc.com/connect"
	context "context"
	errors "errors"
	v1 "github.com/llm-observability/platform/proto/v1"
	http "net/http"
	strings "strings"
)

// This is a compile-time assertion to ensure that this generated file and the connect package are
// compatible. If you get a compiler error that this constant is not defined, this code was
// generated with a version of connect newer than the one compiled into your binary. You can fix the
// problem by either regenerating this code with an older version of connect or updating the connect
// version compiled into your binary.
const _ = connect.IsAtLeastVersion0_1_0

const (
	// SpanIngestionServiceName is the fully-qualified name of the SpanIngestionService service.
	SpanIngestionServiceName = "llm.observability.v1.SpanIngestionService"
)

// These constants are the fully-qualified names of the RPCs defined in this package. They're
// exposed at runtime as Spec.Procedure and as the final two segments of the HTTP route.
//
// Note that these are different from the fully-qualified method names used by
// google.golang.org/protobuf/reflect/protoreflect. To convert from these constants to
// reflection-formatted method names, remove the leading slash and convert the remaining slash to a
// period.
const (
	// SpanIngestionServiceRecordSpanProcedure is the fully-qualified name of the SpanIngestionService's
	// RecordSpan RPC.
	SpanIngestionServiceRecordSpanProcedure = "/llm.observability.v1.SpanIngestionService/RecordSpan"
)

// SpanIngestionServiceClient is a client for the llm.observability.v1.SpanIngestionService service.
type SpanIngestionServiceClient interface {
	RecordSpan(context.Context, *connect.Request[v1.LLMSpan]) (*connect.Response[v1.RecordSpanResponse], error)
}

// NewSpanIngestionServiceClient constructs a client for the
// llm.observability.v1.SpanIngestionService service. By default, it uses the Connect protocol with
// the binary Protobuf Codec, asks for gzipped responses, and sends uncompressed requests. To use
// the gRPC or gRPC-Web protocols, supply the connect.WithGRPC() or connect.WithGRPCWeb() options.
//
// The URL supplied here should be the base URL for the Connect or gRPC server (for example,
// http://api.acme.com or https://acme.com/grpc).
func NewSpanIngestionServiceClient(httpClient connect.HTTPClient, baseURL string, opts ...connect.ClientOption) SpanIngestionServiceClient {
	baseURL = strings.TrimRight(baseURL, "/")
	return &spanIngestionServiceClient{
		recordSpan: connect.NewClient[v1.LLMSpan, v1.RecordSpanResponse](
			httpClient,
			baseURL+SpanIngestionServiceRecordSpanProcedure,
			opts...,
		),
	}
}

// spanIngestionServiceClient implements SpanIngestionServiceClient.
type spanIngestionServiceClient struct {
	recordSpan *connect.Client[v1.LLMSpan, v1.RecordSpanResponse]
}

// RecordSpan calls llm.observability.v1.SpanIngestionService.RecordSpan.
func (c *spanIngestionServiceClient) RecordSpan(ctx context.Context, req *connect.Request[v1.LLMSpan]) (*connect.Response[v1.RecordSpanResponse], error) {
	return c.recordSpan.CallUnary(ctx, req)
}

// SpanIngestionServiceHandler is an implementation of the llm.observability.v1.SpanIngestionService
// service.
type SpanIngestionServiceHandler interface {
	RecordSpan(context.Context, *connect.Request[v1.LLMSpan]) (*connect.Response[v1.RecordSpanResponse], error)
}

// NewSpanIngestionServiceHandler builds an HTTP handler from the service implementation. It returns
// the path on which to mount the handler and the handler itself.
//
// By default, handlers support the Connect, gRPC, and gRPC-Web protocols with the binary Protobuf
// and JSON codecs. They also support gzip compression.
func NewSpanIngestionServiceHandler(svc SpanIngestionServiceHandler, opts ...connect.HandlerOption) (string, http.Handler) {
	spanIngestionServiceRecordSpanHandler := connect.NewUnaryHandler(
		SpanIngestionServiceRecordSpanProcedure,
		svc.RecordSpan,
		opts...,
	)
	return "/llm.observability.v1.SpanIngestionService/", http.HandlerFunc(func(w http.ResponseWriter, r *http.Request) {
		switch r.URL.Path {
		case SpanIngestionServiceRecordSpanProcedure:
			spanIngestionServiceRecordSpanHandler.ServeHTTP(w, r)
		default:
			http.NotFound(w, r)
		}
	})
}

// UnimplementedSpanIngestionServiceHandler returns CodeUnimplemented from all methods.
type UnimplementedSpanIngestionServiceHandler struct{}

func (UnimplementedSpanIngestionServiceHandler) RecordSpan(context.Context, *connect.Request[v1.LLMSpan]) (*connect.Response[v1.RecordSpanResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("llm.observability.v1.SpanIngestionService.RecordSpan is not implemented"))
}
