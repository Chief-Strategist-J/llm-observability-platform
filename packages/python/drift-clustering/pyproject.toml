[project]
name = "drift-clustering"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = []

[tool.ruff]
line-length = 100

[tool.ruff.lint]
select = ["ALL"]

[tool.mypy]
strict = true

[tool.pytest.ini_options]
addopts = "--cov=src --cov-fail-under=80"
