[project]
name = "quality-engine"
version = "1.0.0"
description = "LLM Observability Quality Engine"
readme = "README.md"
authors = [
    { name = "LLM Observability Team" }
]
dependencies = [
    "instrumentation-sdk",
]

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"
