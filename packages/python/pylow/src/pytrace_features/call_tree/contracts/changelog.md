# call_tree contract changelog

## v1 — 2026-06-12
- Initial contract: `calltree` CLI command + `call_tree` MCP tool producing
  exact noise-filtered call trees with file:line per frame, registry-driven
  backends per language.
