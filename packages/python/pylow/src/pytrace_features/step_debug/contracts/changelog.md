# step_debug contract changelog

## v1 — 2026-06-12
- Initial contract: `debug` (interactive, breakpoints pre-set) and
  `debug-steps` (per-breakpoint variable snapshots saved to text files),
  generic engine over declarative DebuggerSpecs.
